Put your two background images here:

- `scene-wide.png` — landscape, used by default and swapped back in on
  `orientation: landscape`.
- `scene-tall.png` — portrait, used under `@media (orientation: portrait)`.
  This should be a separate composition, not a crop of the wide one.
